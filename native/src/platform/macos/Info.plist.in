<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>iTunes-RPC</string>
    <key>CFBundleDisplayName</key>
    <string>iTunes-RPC</string>
    <key>CFBundleIdentifier</key>
    <string>com.hvtim.itunes-rpc</string>
    <key>CFBundleExecutable</key>
    <string>${MACOSX_BUNDLE_EXECUTABLE_NAME}</string>
    <key>CFBundleVersion</key>
    <string>${MACOSX_BUNDLE_BUNDLE_VERSION}</string>
    <key>CFBundleShortVersionString</key>
    <string>${MACOSX_BUNDLE_SHORT_VERSION_STRING}</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <!-- No LSUIElement here on purpose: the tray sets NSApplicationActivationPolicyAccessory
         itself at runtime (main.mm), which covers the same "no Dock icon" behavior without
         hardcoding it into the bundle - keeps main.mm the single source of truth for UI policy. -->
    <key>LSMinimumSystemVersion</key>
    <string>11.0</string>
    <key>NSHighResolutionCapable</key>
    <true/>
</dict>
</plist>
